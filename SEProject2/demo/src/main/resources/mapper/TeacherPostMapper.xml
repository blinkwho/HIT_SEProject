<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sepj.mapper.TeacherPostMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.sepj.entity.TeacherPost">
        <id column="id" property="id" />
        <result column="subject" property="subject" />
        <result column="telephone_number" property="telephoneNumber" />
        <result column="teacher_id" property="teacherId" />
        <result column="price" property="price" />
        <result column="start_date" property="startDate" />
        <result column="end_date" property="endDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, subject, telephone_number, teacher_id, price, start_date,end_date
    </sql>

</mapper>
